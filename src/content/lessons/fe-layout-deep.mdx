---
title: 布局课·排列操练
description: 7 个排列练习——横着排、竖着排、排成网格、固定行/列自动增长、绝对定位。学会「怎么摆」，再记「用什么属性摆」。
slug: fe-layout-deep
status: published
course: frontend-handbook
order: 8
part: basics
durationMinutes: 20
lessonType: lesson
presentation: board
hero:
  line1: 怎么摆
  line2: 比属性名
  badge: 更重要
  pillFill: Flex · Grid · Absolute
  pillOutline: 基础篇 · 排列操练
---

import StepCard from '../../components/course/StepCard.astro';
import StepSplit from '../../components/course/StepSplit.astro';
import SectionHead from '../../components/course/SectionHead.astro';
import Note from '../../components/course/Note.astro';
import Preview from '../../components/course/Preview.astro';
import Takeaway from '../../components/course/Takeaway.astro';

<StepCard num="LAY2-01" label="核心">
  <SectionHead line1="布局就是把块排成你要的样子" line2="先会「怎么摆」，再记「用什么属性」" />
  <Note tag="核心">
    这一课只看排列本身：<strong>横着排、竖着排、排成网格、让某一块占满整行、绝对定位</strong>。你先会「怎么摆」，再去记「用什么属性摆」。
  </Note>
</StepCard>

<StepCard num="LAY2-02" label="一行排列（横排）">
  <StepSplit wide>
    <SectionHead line1="先把元素排成一行" line2="display: flex" />
    <p class="m-0 text-base leading-relaxed">
      {'把容器从默认的 '}
      <code>block</code>
      {' 改成 '}
      <code>flex</code>
      {'，里面几个本来会上下堆叠的块，就会变成左右排开：'}
    </p>
    <div slot="figure" class="flex flex-col gap-4">
      <Preview label="CSS">
        <pre><code>{'.layout-demo {\n  display: flex;\n  flex-direction: row;\n  gap: 12px;\n}'}</code></pre>
      </Preview>
      <Preview label="渲染效果">
        <div style={{ display: 'flex', flexDirection: 'row', gap: '12px', maxWidth: '420px', width: '100%', flexWrap: 'wrap' }}>
          <div style={{ padding: '18px', border: '1px solid #000', background: 'var(--sx-sys-color-bg-surface)', flex: 1, textAlign: 'center', minWidth: '80px' }}>模块 A</div>
          <div style={{ padding: '18px', border: '1px solid #000', background: 'var(--sx-sys-color-bg-surface)', flex: 1, textAlign: 'center', minWidth: '80px' }}>模块 B</div>
          <div style={{ padding: '18px', border: '1px solid #000', background: 'var(--sx-sys-color-bg-surface)', flex: 1, textAlign: 'center', minWidth: '80px' }}>模块 C</div>
        </div>
      </Preview>
    </div>
  </StepSplit>
</StepCard>

<StepCard num="LAY2-03" label="一列排列（竖排）">
  <StepSplit wide>
    <SectionHead line1="把元素排成一列" line2="flex-direction: column" />
    <p class="m-0 text-base leading-relaxed">
      {'还是只改容器这一行：把 '}
      <code>flex-direction</code>
      {' 从 '}
      <code>row</code>
      {' 改成 '}
      <code>column</code>
      {'，元素就会从左右排开变成上下堆叠：'}
    </p>
    <div slot="figure" class="flex flex-col gap-4">
      <Preview label="CSS">
        <pre><code>{'.layout-demo {\n  display: flex;\n  flex-direction: column;\n  gap: 8px;\n}'}</code></pre>
      </Preview>
      <Preview label="渲染效果">
        <div style={{ display: 'flex', flexDirection: 'column', gap: '8px', maxWidth: '300px', width: '100%' }}>
          <div style={{ padding: '18px', border: '1px solid #000', background: 'var(--sx-sys-color-bg-surface)', textAlign: 'center' }}>模块 A</div>
          <div style={{ padding: '18px', border: '1px solid #000', background: 'var(--sx-sys-color-bg-surface)', textAlign: 'center' }}>模块 B</div>
          <div style={{ padding: '18px', border: '1px solid #000', background: 'var(--sx-sys-color-bg-surface)', textAlign: 'center' }}>模块 C</div>
        </div>
      </Preview>
    </div>
  </StepSplit>
</StepCard>

<StepCard num="LAY2-04" label="3×3 卡片">
  <StepSplit wide>
    <SectionHead line1="先把卡片排成 3 列" line2="每列再堆 3 张" />
    <p class="m-0 text-base leading-relaxed">
      {'可以把它理解成'}
      <strong>{'二维数组'}</strong>
      {
        '：外层是 3 个列，每列再放 3 张卡片。前提是你预先知道有几列、每列放哪些卡片。'
      }
    </p>
    <div slot="figure" class="flex flex-col gap-4">
      <Preview label="CSS">
        <pre><code>{'.card-list {\n  display: flex;\n  gap: 12px;\n}\n.card-column {\n  display: flex;\n  flex-direction: column;\n  gap: 12px;\n  flex: 1; /* 三列等宽 */\n}'}</code></pre>
      </Preview>
      <Preview label="渲染效果">
        <div style={{ display: 'flex', gap: '12px', maxWidth: '480px', width: '100%' }}>
          {[0, 1, 2].map(col => (
            <div key={col} style={{ display: 'flex', flexDirection: 'column', gap: '12px', flex: 1 }}>
              {[1, 2, 3].map(n => (
                <div key={n} style={{ padding: '18px', border: '1px solid #000', background: 'var(--sx-sys-color-bg-surface)', textAlign: 'center', minHeight: '50px', display: 'flex', alignItems: 'center', justifyContent: 'center', fontSize: '0.85rem' }}>卡片 {col * 3 + n}</div>
              ))}
            </div>
          ))}
        </div>
      </Preview>
    </div>
  </StepSplit>
</StepCard>

<StepCard num="LAY2-05" label="三列 + 最后一块占满一行">
  <StepSplit wide>
    <SectionHead line1="还是用 flex" line2="flex-wrap + flex-basis: 100%" />
    <p class="m-0 text-base leading-relaxed">
      {'前面三块各占 1/3 宽，最后一块设 '}
      <code>flex-basis: 100%</code>
      {' 独占整行——适合「三张卡 + 下面一段说明」的布局：'}
    </p>
    <div slot="figure" class="flex flex-col gap-4">
      <Preview label="CSS">
        <pre><code>{'.layout-demo {\n  display: flex;\n  flex-wrap: wrap;\n  gap: 12px;\n}\n.layout-box {\n  flex: 0 0 calc((100% - 24px) / 3);\n}\n.layout-box.full {\n  flex-basis: 100%;\n}'}</code></pre>
      </Preview>
      <Preview label="渲染效果">
        <div style={{ display: 'flex', flexWrap: 'wrap', gap: '12px', maxWidth: '420px', width: '100%' }}>
          <div style={{ padding: '18px', border: '1px solid #000', background: 'var(--sx-sys-color-bg-surface)', flex: '0 0 calc((100% - 24px) / 3)', boxSizing: 'border-box', textAlign: 'center' }}>A</div>
          <div style={{ padding: '18px', border: '1px solid #000', background: 'var(--sx-sys-color-bg-surface)', flex: '0 0 calc((100% - 24px) / 3)', boxSizing: 'border-box', textAlign: 'center' }}>B</div>
          <div style={{ padding: '18px', border: '1px solid #000', background: 'var(--sx-sys-color-bg-surface)', flex: '0 0 calc((100% - 24px) / 3)', boxSizing: 'border-box', textAlign: 'center' }}>C</div>
          <div style={{ padding: '18px', border: '1px solid #000', background: 'var(--sx-sys-color-accent)', flexBasis: '100%', boxSizing: 'border-box', textAlign: 'center', fontWeight: 600 }}>D：整行说明</div>
        </div>
      </Preview>
    </div>
  </StepSplit>
</StepCard>

<StepCard num="LAY2-06" label="动态卡片交给 grid">
  <StepSplit wide>
    <SectionHead line1="卡片数量是动态的" line2="display: grid 自动排版" />
    <p class="m-0 text-base leading-relaxed">
      {
        '前面 flex 列式写法是你先手动分组再一列一列渲染。但真实项目里卡片数量经常是动态的——接口回来几张并不确定。这时 '
      }
      <code>grid</code>
      {' 更适合：'}
      <strong>{'你只要说「分成 3 列」，卡片就自动排'}</strong>
      {'。'}
    </p>
    <p class="m-0 text-base leading-relaxed">
      <strong>{'非对称拼贴（bento 风格）'}</strong>
      {'：grid 不只会均匀分栏。你可以让某些块跨行跨列（'}
      <code>grid-column</code>
      {' / '}
      <code>grid-row</code>
      {'），做出像海报的非对称布局：'}
    </p>
    <div slot="figure" class="flex flex-col gap-4">
      <Preview label="CSS（3 等宽列，自动排）">
        <pre><code>{'.layout-demo {\n  display: grid;\n  gap: 12px;\n  grid-template-columns: repeat(3, minmax(0, 1fr));\n}'}</code></pre>
      </Preview>
      <Preview label="渲染效果（9 张卡自动 3×3）">
        <div style={{ display: 'grid', gap: '8px', gridTemplateColumns: 'repeat(3, minmax(0, 1fr))', maxWidth: '480px', width: '100%', fontSize: '0.8rem' }}>
          {Array.from({ length: 9 }, (_, i) => (
            <div key={i} style={{ padding: '14px', border: '1px solid #000', background: 'var(--sx-sys-color-bg-surface)', textAlign: 'center', display: 'flex', alignItems: 'center', justifyContent: 'center', minHeight: '40px' }}>卡片 {i + 1}</div>
          ))}
        </div>
      </Preview>
      <Preview label="渲染效果（非对称拼贴）">
        <div style={{ display: 'grid', gap: '8px', gridTemplateColumns: 'repeat(4, 1fr)', gridTemplateRows: 'repeat(4, 48px)', maxWidth: '400px', width: '100%', fontSize: '0.75rem' }}>
          <div style={{ gridColumn: '1 / 3', border: '1px solid #000', background: 'var(--sx-sys-color-bg-surface)', display: 'flex', alignItems: 'center', justifyContent: 'center' }}>左上横条</div>
          <div style={{ gridColumn: '2 / 4', gridRow: '2 / 4', border: '2px solid #000', background: 'var(--sx-sys-color-accent)', display: 'flex', alignItems: 'center', justifyContent: 'center', fontWeight: 700 }}>中心主卡</div>
          <div style={{ gridColumn: '4', gridRow: '1 / 3', border: '1px solid #000', background: 'var(--sx-sys-color-mint)', display: 'flex', alignItems: 'center', justifyContent: 'center' }}>右侧竖卡</div>
          <div style={{ gridColumn: '1', gridRow: '2 / 4', border: '1px solid #000', background: 'var(--sx-sys-color-lavender)', display: 'flex', alignItems: 'center', justifyContent: 'center' }}>左侧竖卡</div>
          <div style={{ gridColumn: '4', gridRow: '3 / 5', border: '1px solid #000', background: 'var(--sx-sys-color-bg-surface)', display: 'flex', alignItems: 'center', justifyContent: 'center' }}>右下竖卡</div>
          <div style={{ gridColumn: '1 / 3', border: '1px solid #000', background: 'var(--sx-sys-color-bg-surface)', display: 'flex', alignItems: 'center', justifyContent: 'center' }}>左下横条</div>
          <div style={{ gridColumn: '3 / 5', border: '1px solid #000', background: 'var(--sx-sys-color-beige)', display: 'flex', alignItems: 'center', justifyContent: 'center' }}>底部补位</div>
        </div>
      </Preview>
    </div>
  </StepSplit>
</StepCard>

<StepCard num="LAY2-07" label="固定行数，列自动往右长">
  <StepSplit wide>
    <SectionHead line1="行数先定死" line2="列数交给内容自己长" />
    <p class="m-0 text-base leading-relaxed">
      {
        'grid 也能反过来用：你先固定行数（比如固定 2 行），至于最后会长出几列，不用手算——卡片数量到了就自动往右补列。关键是 '
      }
      <code>grid-auto-flow: column</code>
      {'。'}
    </p>
    <Note tag="注意">
      关键镜像：<strong>固定行数 → 新卡片往右长（补列）；固定列数 → 新卡片往下长（补行）</strong>。
    </Note>
    <div slot="figure" class="flex flex-col gap-4">
      <Preview label="CSS">
        <pre><code>{'.layout-demo {\n  display: grid;\n  gap: 12px;\n  grid-template-rows: repeat(2, minmax(0, 1fr)); /* 固定 2 行 */\n  grid-auto-flow: column; /* 新卡片往右长 */\n  grid-auto-columns: minmax(140px, 1fr);\n}'}</code></pre>
      </Preview>
      <Preview label="渲染效果（固定 2 行，7 张卡自动往右补列）">
        <div style={{ display: 'grid', gap: '8px', gridTemplateRows: 'repeat(2, 50px)', gridAutoFlow: 'column', gridAutoColumns: 'minmax(80px, 1fr)', maxWidth: '480px', width: '100%', fontSize: '0.8rem' }}>
          {Array.from({ length: 7 }, (_, i) => (
            <div key={i} style={{ border: '1px solid #000', background: 'var(--sx-sys-color-bg-surface)', display: 'flex', alignItems: 'center', justifyContent: 'center' }}>卡片 {i + 1}</div>
          ))}
        </div>
      </Preview>
    </div>
  </StepSplit>
</StepCard>

<StepCard num="LAY2-08" label="绝对定位">
  <StepSplit wide>
    <SectionHead line1="坐标是相对谁算的" line2="绝对定位先找参考框" />
    <p class="m-0 text-base leading-relaxed">
      {
        'hero 周围「漂」着装饰图标，或右上角固定的「×」关闭按钮——这些都不参与正常文档流。记住一条规则：'
      }
      <strong>{'绝对定位先找「参考框」，再谈 left / top'}</strong>
      {'。'}
    </p>
    <ol class="m-0 pl-5 text-base leading-relaxed">
      <li>
        {'父容器先设 '}
        <code>position: relative</code>
        {'（建立参考框）'}
      </li>
      <li>
        {'子元素设 '}
        <code>position: absolute</code>
      </li>
      <li>
        {'里面每个图标用 '}
        <code>left/top/right/bottom</code>
        {' 定位——坐标都是相对这个参考框算的'}
      </li>
    </ol>
    <Preview slot="figure" label="渲染效果（中心文案 + 四周 orbit 图标）">
      <div style={{ position: 'relative', minHeight: '240px', padding: '40px', border: '1px solid #000', background: 'var(--sx-sys-color-bg-surface)', overflow: 'hidden', width: '100%', maxWidth: '420px', borderRadius: 'var(--sx-sys-radius-md)' }}>
        <div style={{ position: 'relative', zIndex: 2, maxWidth: '260px', margin: '0 auto', textAlign: 'center' }}>
          <p style={{ margin: '0 0 8px', opacity: 0.7, fontSize: '0.8rem' }}>坐标基线 = 整块 hero 画布</p>
          <h3 style={{ margin: '0 0 12px', fontSize: '2rem', lineHeight: 1.1 }}>学习布局</h3>
          <p style={{ margin: 0, fontSize: '0.85rem', opacity: 0.8 }}>中心文案在中间，四周图标各自拿 hero 容器当参考框定位。</p>
        </div>
        {[
          { t: 'Re', left: '14%', top: '10%', bg: 'var(--sx-sys-color-mint)' },
          { t: 'No', left: '4%',  top: '42%', bg: 'var(--sx-sys-color-lavender)' },
          { t: 'Py', left: '18%', top: '70%', bg: 'var(--sx-sys-color-accent)' },
          { t: 'GQ', right: '14%', top: '12%', bg: 'var(--sx-sys-color-beige)' },
          { t: 'Ja', right: '4%',  top: '40%', bg: 'var(--sx-sys-color-accent)' },
          { t: 'Go', right: '12%', bottom: '8%', bg: 'var(--sx-sys-color-mint)' },
        ].map(icon => (
          <div
            key={icon.t}
            style={{
              position: 'absolute',
              width: '44px', height: '44px', borderRadius: '9999px',
              border: '1px solid #000', background: icon.bg,
              display: 'flex', alignItems: 'center', justifyContent: 'center',
              fontWeight: 700, fontSize: '0.8rem',
              ...icon,
            }}
          >{icon.t}</div>
        ))}
      </div>
    </Preview>
  </StepSplit>
</StepCard>

<Takeaway
  num="LAY2-09"
  label="总结"
  line1="一句话记住"
  line2="横排 flex · 网格 grid · 脱离 absolute"
  note="先会「怎么摆」，再去记「用什么属性摆」。"
>
  <ul>
    <li>
      <strong>横排 / 竖排：</strong>
      display: flex + flex-direction
    </li>
    <li>
      <strong>动态卡片网格：</strong>
      display: grid + repeat(N, 1fr)
    </li>
    <li>
      <strong>装饰/角标：</strong>
      父 relative，子 absolute
    </li>
  </ul>
</Takeaway>

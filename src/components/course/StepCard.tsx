import type { ReactNode } from 'react';
interface Props { num: string; label: string; variant?: 'default' | 'reference'; children?: ReactNode }
const styles = `.step{background:transparent;border:none;box-shadow:none;padding:0;display:flex;flex-direction:column;gap:1.125rem;width:100%;box-sizing:border-box;scroll-margin-top:calc(var(--sx-comp-header-logo-size) + 2 * var(--sx-comp-header-padding-block) + 1rem)}.step__head{display:flex;gap:.875rem;align-items:center}.step__num{display:inline-block;padding:5px 10px;background:var(--sx-sys-color-border);color:var(--sx-sys-color-accent);font-family:var(--sx-sys-font-family-mono);font-size:var(--sx-sys-font-size-sm);font-weight:700;letter-spacing:.8px;border:var(--sx-sys-border-width) solid var(--sx-sys-color-border);white-space:nowrap}.step__label{font-family:var(--sx-sys-font-family-display);font-size:var(--sx-sys-font-size-2xl);font-weight:600;line-height:1;color:var(--sx-sys-color-text-primary)}.step__body{display:flex;flex-direction:column;gap:1.125rem}.step__body>p{font-size:var(--sx-sys-font-size-lg);line-height:1.75;color:var(--sx-sys-color-text-soft);margin:0}.step--reference .step__body{gap:.75rem}`;
export default function StepCard({ num, label, variant = 'default', children }: Props) { const id = num.toLowerCase(); return <><style dangerouslySetInnerHTML={{ __html: styles }} /><section className={['step', variant === 'reference' ? 'step--reference' : ''].filter(Boolean).join(' ')} id={id} data-lboard-slide data-num={num} data-label={label}><header className="step__head"><span className="step__num">{num}</span><span className="step__label">{label}</span></header><div className="step__body">{children}</div></section></>; }

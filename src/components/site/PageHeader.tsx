import type { ReactNode } from 'react';
interface Props { title: string; eyebrow?: string; description?: string; children?: ReactNode }
const styles = `.page-head{text-align:center;max-width:var(--sx-comp-page-head-max-width);margin-block:var(--sx-comp-page-head-margin-block-start) var(--sx-comp-page-head-margin-block-end);margin-inline:auto}.page-head__eyebrow{font-family:var(--sx-sys-font-family-mono-system);color:var(--sx-sys-color-action-primary);font-weight:700;letter-spacing:.08em;text-transform:uppercase;font-size:var(--sx-sys-font-size-md)}.page-head__title{font-size:var(--sx-comp-page-head-title-size);font-weight:800;line-height:1.02;letter-spacing:-.03em;margin-block-start:var(--sx-ref-space-2)}.page-head__desc{font-size:var(--sx-comp-page-head-desc-size);line-height:1.6;opacity:.8;margin-block-start:var(--sx-comp-page-head-gap)}.page-head__desc p{margin:0}.page-head__desc code{font-family:var(--sx-sys-font-family-mono-system);font-size:.85em;background:var(--sx-sys-color-bg-surface);border:var(--sx-sys-border-width) solid var(--sx-sys-color-border);padding:.05em .4em}`;
export default function PageHeader({ title, eyebrow, description, children }: Props) { return <><style dangerouslySetInnerHTML={{ __html: styles }} /><header className="page-head">{eyebrow && <p className="page-head__eyebrow">{eyebrow}</p>}<h1 className="page-head__title">{title}</h1><div className="page-head__desc">{description ? <p>{description}</p> : children}</div></header></>; }

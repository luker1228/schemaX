export interface CrumbItem { label: string; href?: string }
interface Props { items: CrumbItem[]; ariaLabel?: string; align?: 'center' | 'start' }
const styles = `.crumb{display:flex;justify-content:center}.crumb--start{justify-content:flex-start}.crumb__pill{display:inline-flex;align-items:center;gap:.5rem;padding:.5rem .75rem;background:var(--sx-sys-color-bg-surface);border:var(--sx-sys-border-width-strong) solid var(--sx-sys-color-border);border-radius:var(--sx-sys-radius-md);box-shadow:var(--sx-sys-shadow-sm);font-family:var(--sx-sys-font-family-mono);font-size:var(--sx-sys-font-size-md);font-weight:700;text-decoration:none}.crumb__link{color:var(--sx-sys-color-text-soft);text-decoration:none;transition:color 120ms ease}.crumb__link:hover,.crumb__link:focus-visible{color:var(--sx-sys-color-text-primary)}.crumb__sep{color:var(--sx-sys-color-text-soft);opacity:.55}.crumb__current{color:var(--sx-sys-color-text-primary)}`;
export default function Crumb({ items, ariaLabel = 'Breadcrumb', align = 'center' }: Props) { return <><style dangerouslySetInnerHTML={{ __html: styles }} /><nav className={['crumb', align === 'start' ? 'crumb--start' : ''].filter(Boolean).join(' ')} aria-label={ariaLabel}><div className="crumb__pill">{items.map((item, index) => { const last = index === items.length - 1; return <span key={`${item.label}-${index}`}>{last ? <span className="crumb__current" aria-current="page">{item.label}</span> : <><a className="crumb__link" href={item.href}>{item.label}</a><span className="crumb__sep" aria-hidden="true"> / </span></>}</span>; })}</div></nav></>; }

---
// 设计系统调参台 island（client:load 保证滑块可拖动）。
// Playground 自带浏览器状态（实时拖动），必须 React island；
// 由 .astro 固定 client:load（MDX / Demo 直接写 client:* 有时会 hydrate 失败）。
import Playground from './Playground.tsx';
---

<Playground client:load />

---
// 设计系统通知演示 island（client:load 保证按钮可触发）。
// Toasts 自带浏览器状态（队列 + 自动消失计时），必须 React island；
// 由 .astro 固定 client:load（MDX / Demo 直接写 client:* 有时会 hydrate 失败）。
import Toasts from './Toasts.tsx';
---

<Toasts client:load />
